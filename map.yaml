controllers:

  ExampleController:
    functions:
      - hello


commands:

  ExampleCommand:
    dir: Example
    parameters:
      - "value:string"
  
  StorageCommand:
    dir: Storage
    parameters:
      - "file:File"
      - "disk:string"
      - "filename:string|null"

      
entities:

  Example:
    values:
      - "value:string"
