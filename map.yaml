controllers:

  ExampleController:
    functions:
      - hello


commands:

  ExampleCommand:
    dir: Example
    parameters:
      - "value:string"

      
entities:

  Example:
    values:
      - "value:string"
